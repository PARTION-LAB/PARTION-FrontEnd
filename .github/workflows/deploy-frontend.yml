name: deploy-frontend

on:
  workflow_run:
    workflows: ["frontend-ci"]
    types:
      - completed

permissions:
  contents: read

jobs:
  deploy:
    name: Deploy static frontend
    runs-on: ubuntu-latest
    environment: production
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'main'

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        env:
          VITE_API_BASE_URL: ""
          VITE_USE_MOCK_API: "false"
          VITE_TOSS_CLIENT_KEY: ${{ secrets.VITE_TOSS_CLIENT_KEY }}
          VITE_DATADOG_RUM_ENABLED: "true"
          VITE_DATADOG_RUM_APPLICATION_ID: ${{ secrets.VITE_DATADOG_RUM_APPLICATION_ID }}
          VITE_DATADOG_RUM_CLIENT_TOKEN: ${{ secrets.VITE_DATADOG_RUM_CLIENT_TOKEN }}
          VITE_DATADOG_RUM_SITE: "ap1.datadoghq.com"
          VITE_DATADOG_RUM_SERVICE: "partion-frontend"
          VITE_DATADOG_RUM_ENV: "prod"
          VITE_DATADOG_RUM_VERSION: ${{ github.event.workflow_run.head_sha }}
          VITE_DATADOG_RUM_SESSION_SAMPLE_RATE: "100"
          VITE_DATADOG_RUM_SESSION_REPLAY_SAMPLE_RATE: "0"
          VITE_DATADOG_RUM_PRIVACY_LEVEL: "mask-user-input"
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Upload static assets
        env:
          S3_BUCKET: ${{ secrets.FRONTEND_S3_BUCKET }}
        run: |
          aws s3 sync dist "s3://${S3_BUCKET}" \
            --delete \
            --exclude "index.html" \
            --cache-control "public,max-age=31536000,immutable"

          aws s3 cp dist/index.html "s3://${S3_BUCKET}/index.html" \
            --cache-control "no-cache,no-store,must-revalidate" \
            --content-type "text/html"

      - name: Invalidate CloudFront cache
        env:
          CLOUDFRONT_DISTRIBUTION_ID: ${{ secrets.FRONTEND_CLOUDFRONT_DISTRIBUTION_ID }}
        run: |
          aws cloudfront create-invalidation \
            --distribution-id "${CLOUDFRONT_DISTRIBUTION_ID}" \
            --paths "/*"
